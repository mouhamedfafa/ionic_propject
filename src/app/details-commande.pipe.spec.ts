import { DetailsCommandePipe } from './details-commande.pipe';

describe('DetailsCommandePipe', () => {
  it('create an instance', () => {
    const pipe = new DetailsCommandePipe();
    expect(pipe).toBeTruthy();
  });
});
