import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic_project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
